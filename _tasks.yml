install-ci:
  summary: install the necessary dependencies to run in CI
  command: |-
    go get \
        golang.org/x/tools/cmd/cover \
        github.com/mattn/goveralls \
        github.com/mitchellh/gox

install:
  summary: install the dependencies to develop locally
  command: go get ./...
